{
  "name": "periodo-layouts",
  "version": "0.0.0",
  "description": "Visual layouts for the PeriodO dataset",
  "main": "index.js",
  "scripts": {
    "prepublish": "rm -rf dist && npm run build",
    "build": "mkdir -p dist && browserify -o dist/bundle.js gallery.js",
    "watch": "mkdir -p dist && watchify -dv -o dist/bundle.js gallery.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "d3": "^4.1.0",
    "immutable": "^3.8.1",
    "n3": "^0.5.0",
    "react": "^15.2.0",
    "react-dom": "^15.2.0",
    "react-hyperscript": "^2.4.0"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "watchify": "^3.7.0"
  }
}
